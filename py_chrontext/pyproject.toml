[tool.maturin]
module-name = "chrontext._chrontext"
python-source = "python"

[project]
name = "chrontext"
dependencies = ["rdflib==6.2.0", "polars==0.18.0", "pyarrow>=7.0.0", "pandas"]

[build-system]
requires = ["maturin>=1.1.0"]
build-backend = "maturin"